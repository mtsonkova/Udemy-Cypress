//runtime environment that executes js code outside the web browser;
//nodejs.org
//the purpose of a module is to be shared with the outside world - export and import the module
//enable ecma script module Add "type" : "module" in package.json file
//statements in Common js module => module.exports and require
//statements in Ecma script => export default and import. In ECMAScript module you can have only one default export with n number of values;
//import data from ".path to module.js"
// for testing purpose use common js module for compatibility
